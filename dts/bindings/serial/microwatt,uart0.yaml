# SPDX-License-Identifier: Apache-2.0

title: MICROWATT UART

description: >
    This binding gives a base representation of the MICROWATT UART

compatible: "microwatt,uart0"

include: uart-controller.yaml

properties:
    reg:
        required: true
